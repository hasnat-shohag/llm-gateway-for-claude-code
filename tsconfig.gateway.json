{
  "//": "The only build config. `npm run build:gateway` compiles gateway-src/*.ts into build/gateway/, which scripts/finalize-gateway-build.js then marks as ESM.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "gateway-src",
    "outDir": "build/gateway"
  },
  "include": ["gateway-src/**/*.ts"],
  "exclude": ["gateway-src/**/*.test.ts"]
}
